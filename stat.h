
/** 
* @brief  
*
* 
* 
* @param  
* @return 
*/
int print_statistics();


/** 
* @brief  Given an array of data and a length, prints the array to the screen
*
* This function basically prints the array on the screen. The array size and the array element are user input.
* 
* @param size of the array and pointer to array 
* @return nothing returned. array is printed on the console
*/

int print_array(unsigned int size_of_array, unsigned char *arr);


/** 
* @brief  Given an array of data and a length, returns the median value
*
* The function takes the data and length of the array and produces median for the array 
* 
* @param  size of the array and pointer to array
* @return median of the array
*/

int find_median(unsigned int size_of_array, unsigned char *arr);

/** 
* @brief  Given an array of data and a length, returns the mean      
*
* The function takes the data and length of the array and producses simple arithmetic average function. a.k.a mean.
* 
* @param  size of the array and pointer to array
* @return mean value of the array 
*/

int find_mean(unsigned int size_of_array, unsigned char *arr));

/** 
* @brief  
*
* 
* 
* @param  
* @return 
*/

int find_maximum();

/** 
* @brief  
*
* 
* 
* @param  
* @return 
*/

int find_minimum();

/** 
* @brief  
*
* 
* 
* @param  
* @return 
*/

int sort_array();


